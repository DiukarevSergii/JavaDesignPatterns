package patterns.creational.singleton.enum_singleton;


// + Serialization
// + Thread safety
// + Ability to use EnumSet,EnumMap,etc.
// + Switch support
// - No lazy initialization

public enum Singleton {
    INSTANCE;
}
